#include <stdio.h>

int main(int argc, char *argv[]) {
	int n = 2; 
	int potencia = 1;
	int max_limite = 100;
	
	while (potencia < max_limite) {
		printf("%d\n", potencia);
		potencia *= n; 
	}
	return 0;
}

